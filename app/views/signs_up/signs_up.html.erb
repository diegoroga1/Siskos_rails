<header>
    <nav>
        <div class="nav-wrapper">
            <a href="/" class="brand-logo"><img class="siskos-logo" src="/assets/siskos.png"></a>
            <ul id="items-menu" class="right ">
		<% if session[:client_id].nil? %>
                	
                	<li id="login" class="waves-effect waves-light btn">
                    	<div class="div-option"><img class="icono-silueta" src="/assets/index/silueta.png"></div>
			<%= link_to 'login'  do %>                    	
			<p>Login</p><br>
                    	
			<%end%>			
                   	</li>
		
		
		<% else %>
		<li  class="waves-effect waves-teal btn">
                    	<div class="div-option"><img class="icono-silueta" src="/assets/index/silueta.png"></div>
         		<%= button_to :action => 'destroy' do %>                    	
				<p>Cerrar </p><br>
                    		<%flash.each do |name,msg| -%>
					<%= content_tag :div,msg, class: name%>
				<%end-%>
			<%end%>	
		</li>	
		<%end%>
		
                <li class="waves-effect waves-light btn">
                    <div class="div-option"><img class="icono-silueta" src="/assets/index/cart.png"></div>
                    <a href="cart"><p>Carrito</p></a>
				</li>
            </ul>
        </div>
    </nav>
</header>

<section>
	<li id="register" class="waves-effect waves-teal ">

		  

		<div><p>Crear cuenta</p></div>
		<ul id="dropdown1" >
			<%= form_for(:client, :url => {:controller => 'signs_up', :action => 'create'}) do |f| %>

				<%= f.text_field :name, :placeholder => "Nombre", :id => "nombre" %>
					<% for message_error in @client.errors[:name] %>
						* <%= message_error %>
					<% end %>
				<%= f.text_field :surname, :placeholder => "Apellidos", :id => "apellidos"%>
					<% for message_error in @client.errors[:surname] %>
					* <%= message_error %>
				<% end %>
				<%= f.email_field :email, :placeholder => "Correo", :id => "correo-registro"%>
					<% for message_error in @client.errors[:email] %>
					* <%= message_error %>
				<% end %>
				<%= f.text_field :address, :placeholder => "Dirección", :id => "direccion"%>
					<% for message_error in @client.errors[:address] %>
					* <%= message_error %>
				<% end %>
				<%= f.password_field :password, :placeholder => "Contraseña", :id => "password-registro"%>
					<% for message_error in @client.errors[:password] %>
					* <%= message_error %>
				<% end %>
				<br>
				<%= f.button :Registrar, :type => "submit", :id => "button-register", :class => "btn center-align"%>
			<% end %>
		</ul>
			<!--<% if @client.errors.any? %>
				<ul class="Signup_Errors">
						<% for message_error in @client.errors.full_messages %>
						<li style="color: red">* <%= message_error %></li>
						<% end %>
				</ul>
			<% end %>-->
		</li>
</section>
<!--Footer-->
<section>
	<%= render "footer" %>
</section>

<!--Fin footer-->